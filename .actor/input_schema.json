{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "DHgate Status Checker",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "DHgate product URLs (Mode: Product) or seller/store URLs (Mode: Seller). Regional hosts such as es.dhgate.com are checked on www.dhgate.com with the matching ship-to country.",
            "editor": "requestListSources",
            "prefill": [
                {
                    "url": "https://www.dhgate.com/product/men-s-polos-luxury-brand-short-sleeve-polo/1064214730.html"
                },
                {
                    "url": "https://www.dhgate.com/store/top-selling/21880856.html"
                }
            ]
        },
        "mode": {
            "title": "Mode",
            "type": "string",
            "description": "What to check: product listings or seller/store pages. Applies to every URL in this run.",
            "editor": "select",
            "default": "product",
            "enum": ["product", "seller"],
            "enumTitles": ["Product", "Seller / Store"]
        },
        "maxRequestsPerCrawl": {
            "title": "Max Requests per Crawl",
            "type": "integer",
            "description": "Maximum number of pages this run may open. Keep it at or above the number of URLs you pass in.",
            "default": 1000,
            "minimum": 1
        },
        "enableProxy": {
            "title": "Use a proxy",
            "type": "boolean",
            "description": "Whether to route the browser through a proxy. When on, the run uses an Apify residential US exit (fixed in code: DHgate is behind Cloudflare, and that exit gets challenged least while giving each retried session a fresh IP). When off, the run goes out on the container's own IP — expect far more 'Verify you are human' challenges.",
            "default": true
        }
    },
    "required": ["startUrls", "mode"]
}
